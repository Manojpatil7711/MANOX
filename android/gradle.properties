org.gradle.jvmargs=-Xmx4G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Flutter's current stable toolchain can enforce the AGP 9 new DSL path.
# MANOX currently uses the legacy Flutter/Android Gradle integration, so
# explicitly opt out until the project is migrated to the new DSL.
android.newDsl=false
