# MANOX release R8/ProGuard rules.
# Keep this file intentionally small: Flutter and Android Gradle Plugin
# provide the baseline rules required by the app and its dependencies.
# Add dependency-specific rules only when a release build demonstrates
# a real reflection/serialization issue.
