# Shared UI widgets

- Reusable UI components (Avatar, LoadingIndicator, EmptyState, ErrorState, AppBar variants).
- Keep widgets small, well-documented and testable. Prefer Keys or semantics labels for stable tests.
