import 'dart:io';
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';
import 'package:image_picker/image_picker.dart';
import '../data/supabase_post_repository.dart';
import 'widgets/media_preview.dart';

class CreatePostPage extends StatefulWidget {
  final bool initialBeat;
  const CreatePostPage({super.key, this.initialBeat = false});
  @override State<CreatePostPage> createState() => _CreatePostPageState();
}

class _CreatePostPageState extends State<CreatePostPage> {
  final ImagePicker _picker = ImagePicker();
  final SupabasePostRepository _repository = SupabasePostRepository();
  final TextEditingController _captionController = TextEditingController();
  XFile? _media;
  late bool _isVideo;
  late bool _isBeat;
  bool _posting = false;
  bool _kidsContent = false;
  String _kidsCategory = 'Science Experiments';
  String _audience = 'Everyone';
  bool _comments = true;
  bool _downloads = true;
  static const List<String> _kidsCategories = ['Science Experiments','Maths','English','History','Geography','GK','Art & Drawing','Music & Dance','Sports','Coding'];
  @override void initState() { super.initState(); _isVideo = widget.initialBeat; _isBeat = widget.initialBeat; }
  @override void dispose() { _captionController.dispose(); super.dispose(); }
  Future<void> _pickMedia({required bool video, required ImageSource source}) async { final XFile? picked = video ? await _picker.pickVideo(source: source, maxDuration: const Duration(minutes: 10)) : await _picker.pickImage(source: source, imageQuality: 90, maxWidth: 2400); if (picked == null || !mounted) return; setState(() { _media = picked; _isVideo = video; if (!video) _isBeat = false; }); }
  Future<void> _openMediaPicker() async { await showModalBottomSheet<void>(context: context, showDragHandle: true, builder: (sheet) => SafeArea(child: Padding(padding: const EdgeInsets.fromLTRB(18,8,18,28), child: Column(mainAxisSize: MainAxisSize.min, children: [Align(alignment: Alignment.centerLeft, child: Text(widget.initialBeat ? 'Upload a BEAT' : 'Add to your post', style: const TextStyle(fontSize: 22, fontWeight: FontWeight.w800))), const SizedBox(height: 16), Row(children: [if (!widget.initialBeat) ...[Expanded(child: _sourceTile(sheet, Icons.photo_library_outlined, 'Photo', () => _pickMedia(video: false, source: ImageSource.gallery))), const SizedBox(width: 10)], Expanded(child: _sourceTile(sheet, Icons.video_library_outlined, 'Video', () => _pickMedia(video: true, source: ImageSource.gallery))), if (!widget.initialBeat) ...[const SizedBox(width: 10), Expanded(child: _sourceTile(sheet, Icons.camera_alt_outlined, 'Photo', () => _pickMedia(video: false, source: ImageSource.camera)))], ]), const SizedBox(height: 10), SizedBox(width: double.infinity, child: OutlinedButton.icon(onPressed: () { Navigator.pop(sheet); _pickMedia(video: true, source: ImageSource.camera); }, icon: const Icon(Icons.videocam_outlined), label: Text(widget.initialBeat ? 'Record BEAT' : 'Record video'))), ])))); }
  Widget _sourceTile(BuildContext sheet, IconData icon, String label, VoidCallback action) => InkWell(onTap: () { Navigator.pop(sheet); action(); }, borderRadius: BorderRadius.circular(16), child: Container(padding: const EdgeInsets.symmetric(vertical: 16), decoration: BoxDecoration(borderRadius: BorderRadius.circular(16), border: Border.all(color: Theme.of(context).dividerColor)), child: Column(children: [Icon(icon,size:28),const SizedBox(height:8),Text(label,style:const TextStyle(fontWeight:FontWeight.w700))])));
  void _openTools() => context.push('/tools');
  Future<void> _publish() async { final XFile? media = _media; final String caption = _captionController.text.trim(); if (media == null && caption.isEmpty) { _show(widget.initialBeat ? 'Select a video to upload your BEAT.' : 'Add a photo, video or caption first.'); return; } if (_isBeat && !_isVideo) { _show('BEATS can only contain video.'); return; } if (_kidsContent && !_isVideo) { _show('Kids content must be a video.'); return; } if (widget.initialBeat && !_isVideo) { _show('Select a video for your BEAT.'); return; } setState(() => _posting = true); try { String? mediaPath; String mediaType = 'post'; if (media != null) { final bytes = await media.readAsBytes(); final extension = media.path.split('.').last.toLowerCase(); if (_isVideo) { mediaPath = await _repository.uploadVideo(bytes, extension, media.mimeType); mediaType = _isBeat ? 'beat' : 'video'; } else { mediaPath = await _repository.uploadImage(bytes, extension, media.mimeType); mediaType = 'image'; } } final visibility = switch (_audience) { 'Followers' => 'followers', 'Only me' => 'private', _ => 'public' }; await _repository.createPost(text: caption, imagePath: mediaPath, mediaType: mediaType, visibility: visibility, audienceCategory: _kidsContent ? 'kids_15_plus' : 'general', kidsCategory: _kidsContent ? _kidsCategory : null, allowComments: _comments, allowDownloads: _isVideo ? _downloads : false); if (mounted) Navigator.of(context).pop(true); } catch (e) { if (!mounted) return; setState(() => _posting = false); _show(e.toString().replaceFirst('Exception: ', '')); } }
  void _show(String message) => ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(message)));
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(leading: IconButton(tooltip:'Close',onPressed:_posting?null:()=>Navigator.of(context).pop(false),icon:const Icon(Icons.close_rounded)), title:Text(widget.initialBeat?'Create BEAT':'Create',style:const TextStyle(fontWeight:FontWeight.w800)), actions:[Padding(padding:const EdgeInsets.only(right:10),child:FilledButton(onPressed:_posting?null:_publish,child:_posting?const SizedBox(width:18,height:18,child:CircularProgressIndicator(strokeWidth:2)):const Text('Post')))]), body: SafeArea(child: ListView(padding:const EdgeInsets.fromLTRB(16,12,16,32),children:[if (_media == null) _emptyMedia() else _mediaPreview(), const SizedBox(height:14), TextField(controller:_captionController,maxLength:2200,maxLines:5,minLines:2,textCapitalization:TextCapitalization.sentences,decoration:InputDecoration(hintText:widget.initialBeat?'Write a BEAT caption…':'Write a caption…',alignLabelWithHint:true,border:const OutlineInputBorder())), const SizedBox(height:8), if (_isVideo && !widget.initialBeat) Card(child:SwitchListTile.adaptive(secondary:const Icon(Icons.music_note_rounded),title:const Text('Add to BEATS',style:TextStyle(fontWeight:FontWeight.w800)),subtitle:const Text('Shows this video in the full-screen BEATS feed'),value:_isBeat,onChanged:_posting?null:(value)=>setState(()=>_isBeat=value))), if (widget.initialBeat) const Card(child:ListTile(leading:Icon(Icons.music_note_rounded),title:Text('BEAT video',style:TextStyle(fontWeight:FontWeight.w800)),subtitle:Text('Full-screen vertical BEATS feed'))), Card(child:Column(children:[ListTile(leading:const Icon(Icons.tune_rounded),title:const Text('Quick edit',style:TextStyle(fontWeight:FontWeight.w800),),subtitle:const Text('Trim • Crop • Filter • Text • Audio'),trailing:const Icon(Icons.chevron_right_rounded),onTap:_posting?null:_openTools),const Divider(height:1),ListTile(leading:const Icon(Icons.people_outline_rounded),title:const Text('Audience'),subtitle:Text(_audience),trailing:const Icon(Icons.chevron_right_rounded),onTap:_posting?null:_showAudience),SwitchListTile.adaptive(secondary:const Icon(Icons.comment_outlined),title:const Text('Comments'),value:_comments,onChanged:_posting?null:(value)=>setState(()=>_comments=value)),if(_isVideo) SwitchListTile.adaptive(secondary:const Icon(Icons.download_outlined),title:const Text('Allow downloads'),value:_downloads,onChanged:_posting?null:(value)=>setState(()=>_downloads=value))])), const SizedBox(height:10), Card(child:SwitchListTile.adaptive(secondary:const Icon(Icons.child_care_rounded),title:const Text('Kids content',style:TextStyle(fontWeight:FontWeight.w800)),subtitle:const Text('Routes this content only to MANOX Kids'),value:_kidsContent,onChanged:widget.initialBeat||_posting?null:(value)=>setState((){_kidsContent=value;if(value)_isBeat=false;}))), if (_kidsContent) Card(child:Padding(padding:const EdgeInsets.fromLTRB(16,8,16,12),child:DropdownButtonFormField<String>(initialValue:_kidsCategory,decoration:const InputDecoration(labelText:'Kids category'),items:_kidsCategories.map((value)=>DropdownMenuItem<String>(value:value,child:Text(value))).toList(),onChanged:_posting?null:(value)=>setState(()=>_kidsCategory=value??_kidsCategory)))), const SizedBox(height:8), TextButton.icon(onPressed:_posting?null:_openTools,icon:const Icon(Icons.build_circle_outlined),label:const Text('Advanced tools')), ])), );
  void _showAudience() { showModalBottomSheet<void>(context:context,showDragHandle:true,builder:(sheet)=>SafeArea(child:Column(mainAxisSize:MainAxisSize.min,children:[const Padding(padding:EdgeInsets.all(18),child:Align(alignment:Alignment.centerLeft,child:Text('Who can see this?',style:TextStyle(fontSize:20,fontWeight:FontWeight.w800)))),for(final option in ['Everyone','Followers','Only me']) ListTile(leading:Icon(_audience==option?Icons.radio_button_checked:Icons.radio_button_off),title:Text(option),onTap:(){setState(()=>_audience=option);Navigator.pop(sheet);}),const SizedBox(height:12)]))); }
  Widget _emptyMedia() => InkWell(onTap:_posting?null:_openMediaPicker,borderRadius:BorderRadius.circular(20),child:Container(height:300,decoration:BoxDecoration(borderRadius:BorderRadius.circular(20),border:Border.all(color:Theme.of(context).dividerColor)),child:Column(mainAxisAlignment:MainAxisAlignment.center,children:[Icon(widget.initialBeat?Icons.video_library_rounded:Icons.add_photo_alternate_outlined,size:56),const SizedBox(height:12),Text(widget.initialBeat?'Upload your BEAT video':'Add photo or video',style:const TextStyle(fontSize:18,fontWeight:FontWeight.w800)),const SizedBox(height:6),const Text('Gallery • Camera • Video',style:TextStyle(fontSize:13))])));
  Widget _mediaPreview() { final media=_media!; return Column(crossAxisAlignment:CrossAxisAlignment.stretch,children:[ClipRRect(borderRadius:BorderRadius.circular(20),child:_isVideo?ManoxLocalVideoPreview(path:media.path,height:430):Image.file(File(media.path),height:430,fit:BoxFit.contain)),const SizedBox(height:10),Row(children:[Expanded(child:OutlinedButton.icon(onPressed:_posting?null:_openTools,icon:const Icon(Icons.tune_rounded),label:const Text('Quick edit'))),const SizedBox(width:10),IconButton.filledTonal(tooltip:'Replace media',onPressed:_posting?null:_openMediaPicker,icon:const Icon(Icons.swap_horiz_rounded))])]); }
}
