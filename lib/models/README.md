Place data models here (Profile, Post, Comment, EarningsEntry, WithdrawalRequest, etc.).

Guidelines:
- Prefer immutable classes with final fields.
- Provide toJson/fromJson for Supabase mapping.
- Add unit tests for model serialization where appropriate.
