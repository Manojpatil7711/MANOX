# Repositories

Repository interfaces and implementations

- Place interfaces for domain repositories (ProfileRepository, PostRepository, EarningsRepository) and their Supabase-backed implementations here.
- Keep business logic and authorization decisions out of widgets.
- Unit test repositories with a Supabase test instance or by mocking the Supabase client.
